{
  "packages/entitlements/src/react/plan-page.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/entitlements/src/react/upsell-host.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/entitlements/src/react/with-entitlement.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/entitlements/src/server/routes.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/mcp/src/guide.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/mcp/src/react/ai-flow-steps.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/mcp/src/react/ai-landing.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/mcp/src/react/ai-onboarding.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/mcp/src/react/ai-status-board.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/mcp/src/react/host-connect-guide.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/backend/src/activation/failure.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/backend/src/activation/verify-redirect.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/backend/src/platform/homologacao-anexo.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/backend/src/platform/homologacao-guide.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/backend/src/providers/infinitepay-setup-guide.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/backend/src/providers/pagbank.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/backend/src/providers/stone-orders.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/backend/src/providers/stone-setup-guide.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/backend/src/providers/stripe-credential-checks.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/backend/src/providers/stripe-setup-guide.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/frontend/src/card/tokenize.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/frontend/src/components/CheckoutPayment.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/frontend/src/components/ConfirmCredentialSave.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/frontend/src/components/ConnectionCard.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/frontend/src/components/CredentialFormAlerts.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/frontend/src/components/EnvironmentTabs.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/frontend/src/components/ProviderConnection.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/frontend/src/components/ProviderList.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/frontend/src/components/ProviderStatusBar.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/frontend/src/components/checkout/apple-pay-button.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/frontend/src/components/checkout/buyer-info-form.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/frontend/src/components/checkout/card-view.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/frontend/src/components/checkout/checkout-steps.tsx": "shipped before the vocabulary gate",
  "packages/payments/frontend/src/components/checkout/google-pay-button.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/frontend/src/components/checkout/method-picker.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/frontend/src/components/checkout/payment-error-panel.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/frontend/src/components/checkout/pix-view.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/frontend/src/components/checkout/providers/hosted-link.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/frontend/src/components/checkout/wallet-pane.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/payments/frontend/src/components/platform/HomologacaoOutcomeCard.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/product-research/src/connectors/fetch-reason.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/product-research/src/connectors/vtex-errors.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/product-research/src/connectors/vtex-validate.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/product-research/src/import/manual.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/product-research/src/normalize/availability.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/product-research/src/normalize/shipping.ts": "connector INPUT grammar, not copy this package emits: the pattern matches a third-party Brazilian retailer's own delivery line so the price can be normalized. Nothing here reaches an adopter's users or types, and translating it would simply stop the parser matching",
  "packages/product-research/src/pipeline/source-budget.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/pwa/src/messages.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/report-builder/src/react/lib/publish-section.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/report-builder/src/react/report-list-archive.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/report-builder/src/react/report-settings-dialog.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/report-builder/src/react/report-view.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/storage/src/problems.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/storage/src/react/create-web-storage.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/storage/src/react/failures.ts": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/ui/src/components/data-display/DataViews/SaveViewModal.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/ui/src/components/data-display/DataViews/data-views-scopes.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both",
  "packages/ui/src/components/form/CategorySelect/CategoryPanelList.tsx": "shipped before the vocabulary gate, and also on the copy ratchet — the same pt-BR sentence names the origin host's domain, so that file's adoption wave clears both"
}
