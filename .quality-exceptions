# Files listed here pre-date the quality gates: their complexity and flakiness
# findings are downgraded to warnings so the gates could ship without a mass
# refactor. Anything NOT listed is a hard error.
#
# The list is empty. It started at 33 entries — 13 source files carrying 84
# complexity findings and 20 test-story files carrying 272 flakiness findings —
# and every one of them has been fixed rather than re-listed.
#
# Adding an entry back means a file is being grandfathered again. Prefer fixing
# the finding; if a construct is genuinely the subject under test, an inline
# disable carrying its reason is narrower than a whole-file exemption.
