# The publishable packages, in TOPOLOGICAL order (dependencies first).
#
# The single source of truth for what this repo releases. ci.yml loads it into
# PUBLISH_DIRS; release-watchdog.yml lets the scripts read it directly; and
# scripts/lib/release-state.mjs falls back to it whenever PUBLISH_DIRS is unset.
#
# Order is load-bearing for PUBLISHING and irrelevant to verifying: a package is
# released only after everything it depends on, so prepare-publish.mjs always
# resolves workspace:* interdeps to a version that already exists.
#
# Paths rather than names because payments ships as a nested workspace.
#
# `packages/discounts` sits LAST because it depends on shared-helpers (the
# search engine its admin list is built on), and shared-helpers is itself last
# among the packages that came before it.
packages/wiring
packages/eslint-config
packages/typescript-config
# `@12-apps/i18n` has no workspace dependencies at all, so its position is free.
# It sits near the front because it is the one every copy-shipping package's
# ADOPTING notes point at, and a reader scanning this list for "what do I need
# first" should meet it before the packages that reference it.
packages/i18n
# `@12-apps/thermal-printing` has no workspace dependencies either — it is pure
# arithmetic plus one node builtin — so its position is free. It sits beside
# i18n for the same reason: a reader scanning for "what needs nothing first"
# should meet both before anything that depends on them.
packages/thermal-printing
packages/feature-flags
packages/request-scope
packages/observability-backend
packages/auth
packages/entity-lifecycle
packages/forms-core
packages/jobs
packages/payments/backend
# billing peers on payments/backend for the failure taxonomy it maps decisions
# onto, so it is released after it.
packages/billing
packages/product-research
packages/rbac
packages/realtime
packages/shift
packages/payments/frontend
packages/payments/e2e
packages/ui
# `@12-apps/email` depends on `@12-apps/ui` for its operator screen, so it sits
# after it. Nothing depends on `email` in turn — it is a leaf, and a host adopts
# it without any other package moving.
packages/email
packages/impersonation
packages/audit
packages/storage
packages/entitlements
packages/pwa
packages/observability-frontend
packages/app-shell
packages/onboarding
packages/product-research-ui
packages/report-builder
packages/notifications
packages/mcp
packages/prisma
packages/shared-helpers
packages/discounts
